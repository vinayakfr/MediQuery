## Steps to recreate

1. Create a virtual environment, make sure to use Python 3.12.x because some packages might not work with the latest version.
2. Install all the packages using the `requirements.txt` file.
3. 